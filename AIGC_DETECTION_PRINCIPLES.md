# AIGC 文本检测原理

更新时间：2026-07-10

本文只说明“文本内容”的 AIGC 检测原理。

项目实际审稿以 `NOVEL_REVIEW_PROTOCOL.md` 为执行正本。本文件后续技术路线用于解释检测方法及其局限，不默认进入章节审稿封包，也不能替代整章文学判断。

## 结论

AIGC 文本检测不是靠某一个固定特征判断，而是综合判断文本是否呈现出机器生成文本常见的统计模式、语言模式和上下文模式。

常见技术路线：

```text
文本统计特征检测
语言模型概率检测
监督分类器检测
零样本检测
文本水印检测
片段级检测
混合写作检测
人工复核
```

检测结果通常是概率或风险判断，不是事实证明。

## 项目审核使用方式

本文件不是“规避检测器”的写作指南，也不把检测分数当成小说质量。项目审核的目标是发现正文为什么不像人物正在经历一件事，而像模型在完成一份大纲。

审查由一个不参与初稿生成的全新审查子智能体执行。它先不看写作者说明和检查项，完整读完正文，用普通读者的话复述：谁想得到什么，谁阻止了他，人物作了什么选择，付出了什么代价，读完最记得哪场戏。无法清楚复述时，先回到故事动力和场景因果，不进入句子润色。

第二遍把本章与最近三个相关章节并读，判断主要解法、信息传播、关系变化载体、情绪释放和结尾压力是否同构。重复判断必须说明场景功能为什么相同，不能依靠关键词、物件计数、句长或固定表达命中。

第三遍才检查语言：人物声音是否趋同，对白是否过度高效，句子是否同样完整光滑，旁白是否替读者解释，生活细节是否只作装饰，参考资料是否仍像被拼接进正文。审查意见必须给出具体位置、读感、场面原因和修法。

故事动力、人物选择、信息路径或场景后果不成立时，应重写章骨或场景；不能用同义替换、删连接词和打乱句长冒充修复。修改后由审查者重新完整阅读，再决定是否通过。本文不作为检测器分数承诺使用。

## 1. 文本统计特征检测

核心思想：AI 文本和人工文本在词汇、句式、节奏和可预测性上可能存在统计差异。

常见观察维度：

- 词汇多样性：是否大量使用常见词、抽象词和泛化表达。
- 句式分布：句长是否过于均匀，句式变化是否不足。
- 段落节奏：段落长度、推进方式和信息密度是否高度稳定。
- 重复模式：是否反复出现相同结构、连接词、总结句和解释句。
- 可预测性：后续词语是否过于容易被语言模型预测。
- burstiness：文本是否缺少人类写作中常见的节奏突变、信息密度变化和表达不均匀性。

这类方法的本质是：如果一段文本过于平滑、整齐、稳定、可预测，就可能被判定为更接近机器生成文本。

局限：

- 规范公文、说明书、教材、新闻稿、网文套路文也可能呈现类似统计特征。
- 高水平人工润色文本也可能显得平滑。
- 短文本样本不足，可靠性较低。

## 2. 语言模型概率检测

核心思想：用语言模型计算文本中每个词或 token 出现的概率，判断这段文本是否像由模型逐步生成。

常见指标：

- perplexity：困惑度。文本越容易被模型预测，困惑度通常越低。
- entropy：熵。文本选择越集中、变化越少，熵可能越低。
- rank / log probability：检查每个词是否经常落在模型最可能输出的候选中。

早期工具会用类似方式判断文本是否“过于符合语言模型偏好”。如果一段文字大量使用模型最容易选择的词和句式，就可能被标为高风险。

局限：

- 不同检测器使用的语言模型不同，判断结果会变。
- 人类写作中高度常规的文本也可能低困惑度。
- 新一代模型生成文本越来越接近人工文本，单纯概率指标越来越不稳定。

## 3. 监督分类器检测

核心思想：收集大量人工文本和 AI 文本，训练一个分类模型，让它学习两类文本的差异。

分类器可能使用：

- 词汇和句法特征。
- 句子之间的语义连贯模式。
- 段落组织方式。
- 主题推进方式。
- 模型内部 embedding。
- 多层神经网络或 Transformer 编码器。

输出通常是一个概率，例如“更像 AI 生成”或“更像人工写作”。

局限：

- 训练集外的题材、语言、文体容易误判。
- 旧检测器可能不适应新模型。
- 中文、网文、诗歌、剧本、方言和强风格文本更难稳定判断。
- 分类器分数不能单独作为定论。

## 4. 零样本检测

核心思想：不专门训练一个二分类器，而是利用语言模型本身的概率、扰动响应或文本曲率来判断。

典型思路：

- DetectGPT 类方法：轻微改写或扰动文本后，观察模型概率变化。如果原文处在模型概率分布的特殊区域，可能更像机器生成。
- Binoculars 类方法：比较两个语言模型对同一文本的概率反应，判断文本是否更像模型生成。
- GLTR 类方法：把每个词在模型候选列表中的排名可视化，观察是否大量落在高概率候选中。

局限：

- 计算成本较高。
- 对检测所用模型依赖强。
- 对经过人工改写、翻译、混合编辑的文本不稳定。

## 5. 文本水印检测

核心思想：生成模型在输出文本时，按特定规则轻微偏向某些 token，使生成文本带有可检测的统计信号。

检测器读取文本后，检查 token 分布是否符合预设水印规则。如果符合，就可以推断文本可能来自某个带水印的生成系统。

优点：

- 如果生成端确实嵌入水印，证据比纯风格判断更强。
- 可用于长文本统计检测。

局限：

- 需要生成模型主动支持水印。
- 文本经过删改、翻译、摘录、重写后，水印信号可能减弱或消失。
- 不同模型、不同平台的水印规则不一定兼容。

## 6. 片段级检测

核心思想：不只判断整篇文章，而是判断哪些段落、句子或片段更像 AI 生成。

常见用途：

- 检测人类文本中夹杂的 AI 段落。
- 标出高风险段落，交给人工复核。
- 判断一篇文章内部风格是否突然变化。

检测方式：

- 滑动窗口：按固定长度切分文本逐段评分。
- 段落分类：每段独立判断，再看整体分布。
- 边界检测：寻找人工写作和 AI 写作之间的转换点。

局限：

- 上下文被切碎后容易误判。
- 人类在不同场景下本来就会改变文风。
- 片段越短，判断越不可靠。

## 7. 混合写作检测

核心思想：现实文本往往不是纯 AI 或纯人工，而是可能经历 AI 起草、人工修改、局部续写、翻译、润色和拼接。

检测目标从“整篇是不是 AI”转向：

- 哪些部分更像机器生成。
- 哪些部分像人工改写。
- 文体变化是否自然。
- 是否存在多个来源或多个写作阶段。

局限：

- 混合写作很难给出单一结论。
- 检测结果更适合作为审查线索，而不是最终证据。

## 8. 人工复核

人工复核不是单纯看“像不像 AI”，而是结合文本和过程证据判断。

可检查：

- 是否有原始构思。
- 是否有大纲和草稿。
- 是否有版本修改记录。
- 是否有符合作者习惯的持续风格。
- 是否存在突然的文体断裂。
- 是否有引用、拼贴或来源不明内容。

对于小说项目，人工复核尤其重要。因为小说正文天然会经历构思、续写、扩写、改写和润色，单次检测分数不能完整反映创作过程。

## 主要局限

- 检测器可能误判人工文本。
- 检测器也可能漏判 AI 文本。
- 短文本、中文文本、强类型文本、套路化文本更难判断。
- 不同检测器可能给出相互矛盾的结果。
- 检测模型会落后于生成模型。
- 分数只能表示风险，不等于事实。

## 对小说文本的特殊影响

小说和普通说明文不同。小说中很多内容本来就可能触发检测器的风险信号：

- 网文类型套路会造成结构重复。
- 人物内心独白可能使用抽象表达。
- 悬疑、修仙、都市等类型文会反复使用固定桥段。
- 章节连载要求会造成固定节奏。
- 同一作者长期写作会有稳定句式和稳定词汇。

因此，小说文本更适合结合创作过程、章节龙骨、草稿记录和人工审校来判断，而不适合只看单个检测分数。

## 参考资料

- OpenAI, New AI classifier for indicating AI-written text: https://openai.com/index/new-ai-classifier-for-indicating-ai-written-text/
- AI-generated text detection: A comprehensive review of methods, datasets, and applications: https://www.sciencedirect.com/science/article/pii/S1574013725000693
